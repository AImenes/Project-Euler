import math

vertexC = [0,0];
vertexA = [-2.5, math.srt(3) * 2.5];
vertexB = [2.5, math.srt(3) * 2.5];

currentPoint = [0,0];
bounce = 0;

while (currentPoint[0] != vertexC[0] && currentPoint[1] != vertexC[1]):
	mail
